# ManiMind 项目架构设计

本文件描述当前仓库已落地的编排架构，不再只是规划草图。

## 零、官方架构图工件

- `docs/architecture.canvas` 是当前仓库的正式架构白板。
- `src/manimind/` 模块边界、角色分布、状态落盘路径、上下文装配链路、第三方能力接入方式发生变化时，必须在同一次修改中同步更新：
  - `docs/architecture.canvas`
  - `docs/通用项目架构模板.md`
- 当前白板粒度固定为“模块/子系统级”，用于表达编排关系、状态流向与外部能力边界，不追求把每个测试文件都画进去。

## 一、当前架构图

```mermaid
flowchart LR
    Manifest["Manifest / 输入清单"]
    Docs["约束文档<br/>AGENTS.md + docs/*"]
    Main["main.py<br/>plan | context-pack | task-update | agent-message | run-to-review | rerun | trace"]
    Frontend["frontend/manimind-console<br/>Next.js control console"]
    Api["backend API<br/>projects | tasks | contexts | events"]
    Bootstrap["bootstrap.py<br/>路径蓝图 / 依赖检查"]
    Models["models.py<br/>schema"]
    Workflow["workflow.py<br/>ProjectPlan / 角色 / DAG / 媒介路由"]
    Context["context_assembly.py<br/>context packet / prompt sections / timing refs"]
    Board["task_board.py<br/>state machine / review gate"]
    Runtime["runtime.py<br/>ProjectRuntime / derive stage"]
    Store["runtime_store.py<br/>snapshots / events"]
    Projects["runtime/projects/&lt;project_id&gt;/"]
    Sessions["runtime/sessions/&lt;session_id&gt;/"]
    Outputs["outputs/&lt;project_id&gt;/"]
    Refs["resources/*"]

    Manifest --> Main
    Docs -.遵循.-> Main
    Docs -.遵循.-> Frontend
    Frontend --> Api
    Main --> Bootstrap
    Main --> Workflow
    Main --> Context
    Main --> Board
    Main --> Store
    Api --> Workflow
    Api --> Context
    Api --> Board
    Workflow --> Models
    Context --> Models
    Board --> Models
    Runtime --> Models
    Store --> Runtime
    Store --> Models
    Bootstrap --> Refs
    Store --> Projects
    Store --> Sessions
    Store --> Outputs
```

## 二、输入与输出

输入：

- 论文 PDF
- 用户笔记
- 目标受众与风格要求
- 镜头清单 manifest（JSON）

输出：

- 结构化项目计划（含角色、上下文、任务图）
- 角色上下文包与提示词分段
- 任务状态推进记录
- 审核报告与输出资产清单

## 三、架构分层

```text
预启动层
  ├─ 文档与配置加载
  ├─ 运行时依赖检测（resources + tools）
  └─ 路径蓝图生成（runtime/projects, runtime/sessions, outputs）

前端控制台层（frontend/manimind-console/）
  ├─ src/app              App Router 入口与页面壳
  ├─ src/components/console 控制台首页布局与业务卡片
  ├─ src/components/ui    轻量展示组件
  ├─ src/data             API 响应映射与展示态组装
  └─ 只通过 backend API 访问编排状态

编排核心层（src/manimind/）
  ├─ models.py            数据模型：角色、上下文、任务、状态
  ├─ workflow.py          角色编排、上下文蓝图、任务依赖图、审核关卡
  ├─ ingest.py            输入摄取（md/txt/pdf，PDF 可接本地 `pdf/` skill 增强）
  ├─ prompt_system.py     独立提示词系统与角色配方
  ├─ llm_client.py        primary/review/worker 三路 Responses API 路由
  ├─ worker_adapters.py   HTML/SVG/Manim 执行适配层
  ├─ executor.py          各 stage 实现（ingest/summarize/plan/dispatch/review）
  ├─ stage_orchestrator.py Stage Runner 编排入口（run_all / rerun）
  ├─ stages/              StageRunner 协议与分阶段 runner
  ├─ contract_store.py    JSON Schema 契约加载与校验
  ├─ trace_store.py       会话级 trace 落盘与项目级 trace-summary 聚合
  ├─ review_workflow.py   人工审核（approve/return）
  ├─ post_produce.py      后处理与交付打包（当前字幕/音频/视频/manifest；下一轮拆分音频前置）
  ├─ tts.py               TTS 适配层（powershell_sapi/command/f5_tts/noop，下一轮纳入语速与 part 时长）
  ├─ context_assembly.py  上下文包与提示词分段装配（含缓存）
  ├─ task_board.py        任务状态机（owner/blocker/verification nudge）
  ├─ runtime.py           ProjectRuntime 加载、状态回填与阶段派生
  ├─ runtime_store.py     项目状态、上下文包、任务更新的落盘与日志
  ├─ bootstrap.py         初始化、外部路径检查、参考归档检查
  └─ main.py              CLI 入口（plan/context-pack/task-update/agent-message/run-to-review/rerun/trace/human-review/finalize）

API 层（backend/）
  ├─ main.py              FastAPI 入口
  └─ api/*                projects/tasks/contexts/events/execution/reviews 路由

执行能力层（第三方）
  ├─ pdf/                               本地 PDF ingest 增强 skill（OCR / 表格 / 表单 / 图像）
  ├─ resources/skills/html-animation/
  ├─ resources/references/hyperframes/
  └─ resources/skills/manim/SKILL.md
```

## 四、角色架构（已实现）

角色通过 `AgentMode` 三态约束行为：

- `read_only`：只读探索或规划，不允许产物写入
- `structured_write`：允许写入结构化产物，必须遵循归属
- `verify_only`：只输出审核结论，不参与内容生产

默认角色集：

- `lead`
- `explorer`
- `planner`
- `coordinator`
- `html_worker`
- `manim_worker`
- `svg_worker`
- `reviewer`
- `human_reviewer`

每个角色都有：

- `allowed_stages`
- `required_inputs`
- `owned_outputs`
- `output_contract`

实现位置：

- 角色画像：`workflow.build_agent_profiles(...)`
- 角色提示词配方：`prompt_system.py`

## 五、状态机与任务图（已实现）

阶段状态机：

```text
PRESTART -> INGEST -> SUMMARIZE -> PLAN -> DISPATCH -> REVIEW -> POST_PRODUCE -> PACKAGE -> DONE
                                                \-> BLOCKED
```

任务级状态机（`ExecutionTask`）：

- 状态：`pending` / `in_progress` / `completed`
- 约束：`blocked_by` 必须全部完成后，任务才可推进
- 所有权：只有 `owner_role` 或 `lead` 可推进任务
- 强制审核：`review.outputs` 为验证关卡，未完成不得进入后处理
- 阻塞表达：`blocked` 通过依赖与事件原因表达，不额外扩充任务状态枚举
- 查询增强（阶段 1 约定）：收到 `worker.blocker` 时，可在任务快照补 `blocked_reason` / `blocked_at`

实现位置：`task_board.list_available_tasks(...)` 与 `task_board.update_execution_task_status(...)`。

## 六、上下文装配（已实现）

上下文记录由 `ContextRecord` 建模，字段包括：

- `scope`（长期/短期）
- `writer_role`
- `consumer_roles`
- `lifecycle`
- `invalidation_rule`
- `sticky`

上下文注入由 `build_context_packet(plan, role_id, stage)` 实现：

- 按角色模式 + 角色必需输入选择上下文
- 返回统一 `constraints` 与 `write_targets`
- 可选渲染提示词分段（`PromptSectionCache`）

静态/动态双态载体（阶段 1 基线）：

- 静态上下文：项目级稳定事实与已批准条目，适合沉淀到 `runtime/projects/<project_id>/`
- 动态上下文：按角色、阶段、会话装配的运行时材料，适合沉淀到 `runtime/sessions/<session_id>/`
- 提升链路：子 Agent 先回写动态材料，再由 leader 提交为静态事实或阶段推进
- 进度写入（阶段 1 约定）：`worker.progress` 默认只追加到会话级 `events.jsonl`，不生成独立 progress 文件

## 七、CLI 接口（已实现）

- `plan <manifest>`：构建完整项目计划
- `context-pack <manifest> <role_id> <stage>`：输出角色上下文包
- `context-pack ... --render-prompt-sections`：输出提示词分段
- `task-update <manifest> <task_id> <status> <actor_role>`：按状态机推进任务
- `agent-message <manifest> <event_type> <role_id> <stage>`：写入 `worker.progress` / `worker.blocker` / `worker.result` / `review.decision`
- `run-to-review <manifest>`：按 Stage Runner 依次推进到 `review.draft`
- `rerun <manifest> <runner_name> [--segment <segment_id>]`：按 runner 重跑（例如 `rerun plan`）
- `trace <manifest> --session-id <session_id> [--stage] [--role] [--failed-only]`：查询会话级 LLM trace
- 上述命令均支持 `--session-id`，并会把结果落盘到 `runtime/projects/...` 与 `runtime/sessions/...`

## 八、与 ClaudeCode 的关系

- `ClaudeCode/` 只保留为可选参考归档，不是运行时依赖。
- 已抽取并固化到 ManiMind 的能力：
  - 角色模式化编排
  - 上下文分段装配与缓存
  - 任务依赖状态机与验证关卡

对应清单见 [ClaudeCode抽取清单.md](./ClaudeCode抽取清单.md)。
## 阶段 1 补充约束（2026-05-10）

1. `reviewer` 与 `human_reviewer` 角色必须拆分：
- `reviewer` 只产出审核草案（draft）。
- `human_reviewer` 才能提交最终 `approve/return`。

2. 人工 `return` 必须同时落盘两类信息：
- 会话级反馈记录：`runtime/sessions/<session_id>/review-returns/latest.json`
- Prompt 注入内容：在 `context-pack` 的独立 `human_review_feedback` 段注入，不得混入普通 summary。

3. `review.outputs` 任务完成条件：
- owner 为 `human_reviewer`
- `required_outputs` 至少包含 `<project_id>.review.report`
- 未满足条件不得推进 `post_produce.outputs`

## 阶段 1.1 补充约束（2026-05-11 首跑反馈，待实现）

1. 配音链路必须前移：
- 在 `narration.script` 产出后先执行配音，再进入视觉制作。
- 每个 part 的真实配音时长要沉淀为 timing manifest，供 HTML / Manim / SVG 消费。

2. 媒介路由必须收敛：
- `manim_worker` 负责公式核心、推导和证明可视化。
- `html_worker` 负责开头引子、段间衔接、弱科普说明和轻量信息卡。
- `svg_worker` 仅在明确需要关系图或增强覆盖层时启用，不得默认全跑。

3. 分镜完整性必须成为审核硬约束：
- `storyboard_master` 至少覆盖 `intro -> formula_core/bridge -> summary` 的叙事骨架。
- 没有引子、没有桥接、只有公式展示的成片不得通过审核。

